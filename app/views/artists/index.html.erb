<div class="container">

<h1>All Artists</h1>


<h4><%= link_to "New Artist", new_artist_path %></h4>


<div class="artists_listing">
  <ul>
    <% @artists.each do |artist| %>
      <li><%= link_to artist.name, artist_path(artist) %></li>
      <% end %>
    </ul>
  </div>

</div>

</div>
