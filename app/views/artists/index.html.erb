<div class="container">

<h1>All Artists</h1>


<h4><%= link_to "New Artist", new_artist_path, class: "btn btn-primary" %></h4>


<table class="table table-hover">
    <% @artists.each do |artist| %>
      <tr>
      <td><%= link_to artist.name, artist_path(artist) %></td>
      <% end %>
    </tr>
    <tr><td></td></tr>
  </table>

</div>

</div>
